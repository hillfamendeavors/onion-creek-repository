<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Trusted Neighbors | Hyper-Local Neighborhood Directories</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

  <!-- SEO Meta Tags -->
  <meta name="description"
    content="Authentic, neighbor-recommended business directories for local communities. No sponsored ads, no fake reviews. Sourced directly from neighbors for Onion Creek, Sunfield, and Circle C." />
  <meta name="keywords"
    content="neighborhood directory, local recommendations, trusted neighbors, local business referrals, Onion Creek directory, Sunfield, Circle C, Austin local directory" />

  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website" />
  <meta property="og:title" content="Trusted Neighbors | Hyper-Local Neighborhood Directories" />
  <meta property="og:description"
    content="Skip the sponsored ads and fake online reviews. Find authentic, neighbor-recommended businesses in your community." />
  <meta property="og:url" content="https://trustedneighbors.net/" />

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Lexend+Exa:wght@300;400;500;600;700;800;900&family=Raleway:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,400&display=swap"
    rel="stylesheet" />

  <style is:global>
    :root {
      /* Premium Branding Color System (Forest Green & Gold) */
      --color-primary: #0f2e22;
      /* Rich dark forest green */
      --color-primary-mid: #1b4d3a;
      /* Medium forest green */
      --color-primary-light: #2c6e54;
      /* Light forest green */
      --color-accent: #d4af37;
      /* Refined gold */
      --color-accent-hover: #e5c655;
      /* Light gold for interactions */
      --color-accent-pale: #fdfaf2;
      /* Soft cream gold background */
      --color-neutral-dark: #202624;
      /* Warm dark grey/black */
      --color-neutral-light: #faf9f5;
      /* Clean warm off-white */
      --color-neutral-white: #ffffff;
      --color-border: #e2dfd5;
      /* Warm light grey border */

      /* Visit Austin Inspired Palette Accents */
      --color-austin-brown: #ab5744;
      /* Terracotta */
      --color-austin-salmon: #f48a85;
      /* Highlight salmon */
      --color-austin-yellow-tint: #f3f4d6;
      /* Soft yellow-green tint */
      --color-austin-brown-tint: #efe3de;
      /* Warm cream background tint */

      /* Accent colors for neighborhood branding */
      --color-onion: #114d33;
      /* Green identity */
      --color-sunfield: #c86a4a;
      /* Sienna/rust identity */
      --color-circlec: #0f3b3f;
      /* Deep teal identity */
      --color-averyranch: #1a365d;
      /* Deep navy identity */

      /* Animation curves */
      --transition-smooth: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      scroll-behavior: smooth;
      overflow-x: hidden;
    }

    body {
      font-family: 'Raleway', sans-serif;
      background: var(--color-neutral-light);
      color: var(--color-neutral-dark);
      min-height: 100vh;
      line-height: 1.6;
      overflow-x: hidden;
      width: 100%;
    }

    h1,
    h2,
    h3,
    h4 {
      font-family: 'Lexend Exa', sans-serif;
      font-weight: 800;
      color: var(--color-primary);
    }

    /* TOP UTILITY BAR */
    .top-bar {
      background: var(--color-austin-yellow-tint);
      color: var(--color-primary);
      padding: 8px 40px;
      font-size: 0.65rem;
      font-weight: 800;
      letter-spacing: 2px;
      text-transform: uppercase;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1.5px solid var(--color-primary);
    }

    .top-bar-links {
      display: flex;
      gap: 20px;
    }

    .top-bar-links a {
      color: var(--color-primary);
      text-decoration: none;
      transition: var(--transition-smooth);
    }

    .top-bar-links a:hover {
      color: var(--color-austin-brown);
    }

    /* MAIN NAV BAR (Glassmorphism) */
    .main-nav {
      background: rgba(250, 249, 245, 0.8);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      padding: 16px 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 1.5px solid var(--color-primary);
      position: sticky;
      top: 0;
      z-index: 100;
      box-shadow: 0 4px 30px rgba(0, 0, 0, 0.03);
    }

    .logo-container {
      display: flex;
      align-items: center;
      text-decoration: none;
      gap: 10px;
    }

    .logo-badge {
      background: var(--color-accent);
      color: var(--color-primary);
      width: 32px;
      height: 32px;
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Lexend Exa', sans-serif;
      font-weight: 900;
      font-size: 1rem;
      border: 1.5px solid var(--color-primary);
      box-shadow: 2px 2px 0px var(--color-primary);
    }

    .main-nav-logo {
      font-size: 1.25rem;
      font-weight: 800;
      letter-spacing: -0.5px;
      color: var(--color-primary);
    }

    .main-nav-logo span {
      color: var(--color-accent);
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 32px;
    }

    .nav-links a {
      font-family: 'Lexend Exa', sans-serif;
      font-weight: 700;
      font-size: 0.72rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      color: var(--color-primary);
      text-decoration: none;
      transition: var(--transition-smooth);
      position: relative;
    }

    .nav-links a::after {
      content: '';
      position: absolute;
      width: 0;
      height: 2px;
      bottom: -4px;
      left: 0;
      background-color: var(--color-accent);
      transition: var(--transition-smooth);
    }

    .nav-links a:hover::after {
      width: 100%;
    }

    .nav-links a:hover {
      color: var(--color-accent-hover);
    }

    .nav-cta {
      background: var(--color-accent);
      color: var(--color-primary) !important;
      padding: 8px 18px;
      border-radius: 6px;
      border: 1.5px solid var(--color-primary);
      box-shadow: 2px 2px 0px var(--color-primary);
    }

    .nav-cta::after {
      display: none !important;
    }

    .nav-cta:hover {
      background: var(--color-accent-hover) !important;
      color: var(--color-primary) !important;
      transform: translate(-1px, -1px);
      box-shadow: 3px 3px 0px var(--color-primary);
    }

    /* MOBILE NAVIGATION */
    .menu-toggle { display: none; }

    .menu-btn {
      display: none;
      cursor: pointer;
      background: transparent;
      border: 1.5px solid var(--color-primary);
      width: 42px;
      height: 42px;
      border-radius: 6px;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 5px;
      box-shadow: 2px 2px 0px var(--color-primary);
      transition: background 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
      position: relative;
    }

    .menu-bar {
      display: block;
      width: 18px;
      height: 2px;
      background: var(--color-primary);
      border-radius: 2px;
      transform-origin: center;
      transition: transform 0.35s cubic-bezier(0.23, 1, 0.32, 1),
                  opacity 0.25s ease,
                  width 0.3s ease,
                  background 0.25s ease;
    }

    /* Full-screen backdrop when menu open */
    .menu-backdrop {
      position: fixed;
      inset: 0;
      background: rgba(15, 46, 34, 0.45);
      backdrop-filter: blur(4px);
      -webkit-backdrop-filter: blur(4px);
      z-index: 99;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.35s ease;
      cursor: pointer;
    }

    /* HERO SECTION */
    .hero {
      position: relative;
      padding: 100px 40px 100px;
      background: linear-gradient(135deg, #0b2219 0%, #153c2e 100%);
      color: var(--color-neutral-white);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .hero::before {
      content: '';
      position: absolute;
      inset: 0;
      background: url('/assets/Downtown_Skyline_Upscale_Paulina_Skarbowska_Wiewior_bateyes_atx_Lifetime_Usage__4469f6fe-40d5-4df8-8fee-22b3b75c9862.jpg') no-repeat center center;
      background-size: cover;
      opacity: 0.15;
      z-index: 1;
    }

    .hero-container {
      position: relative;
      z-index: 2;
      max-width: 1200px;
      width: 100%;
      display: grid;
      grid-template-columns: 1.2fr 0.8fr;
      gap: 60px;
      align-items: center;
    }

    .hero-content .hero-eyebrow {
      animation: fadeInUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) 0.1s both;
    }

    .hero-content h2 {
      animation: fadeInUp 0.7s cubic-bezier(0.16, 1, 0.3, 1) 0.25s both;
    }

    .hero-content .hero-subtitle {
      animation: fadeInUp 0.7s cubic-bezier(0.16, 1, 0.3, 1) 0.42s both;
    }

    .hero-content .hero-ctas {
      animation: fadeInUp 0.65s cubic-bezier(0.16, 1, 0.3, 1) 0.58s both;
    }

    .hero-eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--color-accent);
      margin-bottom: 20px;
      background: rgba(212, 175, 55, 0.1);
      padding: 6px 16px;
      border-radius: 4px;
      border: 1.5px solid var(--color-accent);
    }

    .hero h2 {
      font-size: clamp(2.2rem, 4.5vw, 3.5rem);
      font-weight: 800;
      line-height: 1.1;
      color: var(--color-neutral-white);
      margin-bottom: 24px;
      letter-spacing: -1px;
    }

    .hero h2 span {
      background: linear-gradient(to right, var(--color-accent), #f3e5ab);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .hero-subtitle {
      font-size: 1.15rem;
      font-weight: 300;
      color: rgba(255, 255, 255, 0.85);
      margin-bottom: 40px;
      max-width: 600px;
      line-height: 1.6;
    }

    .hero-ctas {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 12px 24px;
      font-family: 'Lexend Exa', sans-serif;
      font-weight: 700;
      font-size: 0.8rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      text-decoration: none;
      border-radius: 6px;
      border: 1.5px solid var(--color-primary);
      transition: var(--transition-smooth);
      cursor: pointer;
    }

    .btn-primary {
      background: var(--color-accent);
      color: var(--color-primary);
      box-shadow: 3px 3px 0px var(--color-primary);
    }

    .btn-primary:hover {
      background: var(--color-accent-hover);
      transform: translate(-1px, -1px);
      box-shadow: 4px 4px 0px var(--color-primary);
    }

    .btn-secondary {
      background: transparent;
      color: var(--color-neutral-white);
      border-color: rgba(255, 255, 255, 0.4);
      box-shadow: 3px 3px 0px rgba(255, 255, 255, 0.2);
    }

    .btn-secondary:hover {
      background: rgba(255, 255, 255, 0.08);
      border-color: var(--color-neutral-white);
      transform: translate(-1px, -1px);
      box-shadow: 4px 4px 0px var(--color-neutral-white);
    }

    /* HERO IMAGE GRAPHIC (Mockup style) */
    .hero-graphic {
      position: relative;
      width: 100%;
      height: 400px;
      display: flex;
      justify-content: center;
      align-items: center;
      animation: fadeInRight 1s cubic-bezier(0.16, 1, 0.3, 1) 0.4s both;
    }

    .hero-image-card {
      background: var(--color-neutral-white);
      padding: 12px;
      border-radius: 6px;
      box-shadow: 4px 4px 0px var(--color-primary);
      width: 85%;
      height: 90%;
      transform: rotate(3deg);
      border: 1.5px solid var(--color-primary);
      position: relative;
    }

    .hero-image-card::after {
      content: '';
      position: absolute;
      inset: 12px;
      background: url('/assets/Downtown_Skyline_Upscale_Paulina_Skarbowska_Wiewior_bateyes_atx_Lifetime_Usage__4469f6fe-40d5-4df8-8fee-22b3b75c9862.jpg') no-repeat center center;
      background-size: cover;
      border-radius: 4px;
      filter: saturate(1.1) brightness(0.9);
      border: 1.5px solid var(--color-primary);
    }

    .badge-floating {
      position: absolute;
      top: -20px;
      left: -20px;
      background: var(--color-austin-salmon);
      color: var(--color-primary);
      border: 1.5px solid var(--color-primary);
      padding: 12px 18px;
      border-radius: 50%;
      width: 105px;
      height: 105px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.6rem;
      font-weight: 800;
      text-transform: uppercase;
      text-align: center;
      line-height: 1.2;
      box-shadow: 3px 3px 0px var(--color-primary);
      transform: rotate(-10deg);
      z-index: 10;
    }

    .badge-floating strong {
      font-size: 1.1rem;
      color: var(--color-primary);
      display: block;
    }

    /* TRUST / STATS BAR */
    .trust-bar {
      position: relative;
      padding: 56px 40px;
      background: linear-gradient(135deg, #0d2a1f 0%, #11392b 100%);
      border-top: 3px solid var(--color-accent);
      border-bottom: 1px solid rgba(212, 175, 55, 0.25);
      color: var(--color-neutral-white);
      text-align: center;
      overflow: hidden;
    }

    .trust-bar-inner {
      position: relative;
      z-index: 2;
      max-width: 1000px;
      margin: 0 auto;
    }

    .trust-eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.7rem;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--color-accent);
      background: rgba(212, 175, 55, 0.1);
      padding: 6px 16px;
      border-radius: 4px;
      border: 1.5px solid var(--color-accent);
      margin-bottom: 32px;
    }

    .trust-stats {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-wrap: wrap;
      margin-bottom: 28px;
    }

    .trust-stat {
      padding: 8px 56px;
    }

    .trust-stat + .trust-stat {
      border-left: 1px solid rgba(212, 175, 55, 0.3);
    }

    .trust-number {
      display: block;
      font-family: 'Lexend Exa', sans-serif;
      font-weight: 800;
      font-size: clamp(2.4rem, 5vw, 3.4rem);
      line-height: 1;
      letter-spacing: -1px;
      background: linear-gradient(to right, var(--color-accent), #f3e5ab);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .trust-label {
      display: block;
      margin-top: 10px;
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: rgba(255, 255, 255, 0.7);
    }

    .trust-tagline {
      font-size: 1.1rem;
      font-weight: 300;
      color: rgba(255, 255, 255, 0.85);
      max-width: 560px;
      margin: 0 auto;
    }

    @media (max-width: 600px) {
      .trust-bar { padding: 44px 20px; }
      .trust-stat { width: 100%; padding: 16px 20px; }
      .trust-stat + .trust-stat {
        border-left: none;
        border-top: 1px solid rgba(212, 175, 55, 0.2);
      }
    }

    /* SECTION LAYOUTS */
    section {
      padding: 100px 40px;
      display: flex;
      justify-content: center;
    }

    .section-container {
      max-width: 1200px;
      width: 100%;
    }

    .section-header {
      text-align: center;
      margin-bottom: 60px;
      max-width: 700px;
      margin-left: auto;
      margin-right: auto;
    }

    .section-eyebrow {
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--color-austin-brown);
      margin-bottom: 12px;
      display: block;
    }

    .section-title {
      font-size: 1.8rem;
      font-weight: 800;
      color: var(--color-primary);
      margin-bottom: 16px;
      letter-spacing: -0.5px;
    }

    .section-desc {
      font-size: 1.05rem;
      color: #606664;
    }

    /* HOW IT WORKS SECTION */
    .how-it-works {
      background: var(--color-austin-brown-tint);
      border-bottom: 1.5px solid var(--color-primary);
    }

    .steps-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 40px;
    }

    .step-card {
      background: var(--color-neutral-white);
      padding: 40px 30px;
      border-radius: 6px;
      border: 1.5px solid var(--color-primary);
      position: relative;
      box-shadow: 3px 3px 0px rgba(15, 46, 34, 0.15);
      transition: var(--transition-smooth);
    }

    .step-card:hover {
      transform: translate(-2px, -2px);
      box-shadow: 5px 5px 0px var(--color-primary);
      border-color: var(--color-primary);
    }

    .step-number {
      font-family: 'Lexend Exa', sans-serif;
      font-size: 3rem;
      font-weight: 800;
      color: rgba(15, 46, 34, 0.1);
      line-height: 1;
      position: absolute;
      top: 24px;
      right: 30px;
    }

    .step-icon {
      font-size: 2.2rem;
      margin-bottom: 20px;
      display: inline-block;
    }

    .step-title {
      font-size: 1.25rem;
      margin-bottom: 12px;
      font-weight: 700;
    }

    .step-text {
      font-size: 0.95rem;
      color: #555c5a;
      line-height: 1.5;
    }

    /* NEIGHBORHOOD DIRECTORIES SECTION */
    .neighborhoods-section {
      background: var(--color-neutral-light);
    }

    .panel-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(min(320px, 100%), 1fr));
      gap: 30px;
    }

    .selector-card {
      display: flex;
      flex-direction: column;
      background: var(--color-neutral-white);
      border-radius: 6px;
      border: 1.5px solid var(--color-primary);
      box-shadow: 4px 4px 0px rgba(15, 46, 34, 0.15);
      overflow: hidden;
      text-decoration: none;
      color: inherit;
      transition: var(--transition-smooth);
    }

    .selector-card:hover {
      transform: translate(-3px, -3px);
      box-shadow: 7px 7px 0px var(--color-primary);
    }

    .card-top {
      padding: 40px 30px;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      min-height: 200px;
      transition: var(--transition-smooth);
      position: relative;
    }

    .card-tag {
      font-size: 0.8rem;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-bottom: 12px;
    }

    .card-title {
      font-size: 2.2rem;
      font-weight: 800;
      line-height: 1.15;
      text-transform: uppercase;
      letter-spacing: -0.5px;
      margin-bottom: 8px;
      color: var(--color-neutral-white);
    }

    .card-desc {
      font-size: 0.95rem;
      opacity: 0.85;
      font-weight: 300;
    }

    .card-footer {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 24px 30px;
      background: #faf9f5;
      border-top: 1.5px solid var(--color-primary);
    }

    .cta-label {
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      color: #606664;
    }

    .cta-button {
      display: inline-block;
      padding: 8px 16px;
      border-radius: 4px;
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 1px;
      text-transform: uppercase;
      text-align: center;
      border: 1.5px solid var(--color-primary);
      transition: var(--transition-smooth);
      box-shadow: 2px 2px 0px var(--color-primary);
    }

    /* Neighborhood specific theme colors */
    .selector-card.onion .card-top {
      background: linear-gradient(135deg, #114D33 0%, #2A6B45 100%);
      color: var(--color-neutral-white);
    }

    .selector-card.onion .card-tag {
      color: var(--color-accent);
    }

    .selector-card.onion .cta-button {
      color: #114D33;
      background: var(--color-accent);
    }

    .selector-card.onion:hover .card-top {
      background: linear-gradient(135deg, #0e3e29 0%, #215335 100%);
    }

    .selector-card.onion:hover .cta-button {
      background: var(--color-neutral-white);
      color: #114d33;
      border-color: var(--color-primary);
      transform: translate(-1px, -1px);
      box-shadow: 3px 3px 0px var(--color-primary);
    }

    .selector-card.sunfield .card-top {
      background: linear-gradient(135deg, #C86A4A 0%, #A65646 100%);
      color: var(--color-neutral-white);
    }

    .selector-card.sunfield .card-tag {
      color: #fce8e2;
    }

    .selector-card.sunfield .cta-button {
      color: #A65646;
      background: #fce8e2;
    }

    .selector-card.sunfield:hover .card-top {
      background: linear-gradient(135deg, #b05c3f 0%, #8c4739 100%);
    }

    .selector-card.sunfield:hover .cta-button {
      background: var(--color-neutral-white);
      color: #A65646;
      border-color: var(--color-primary);
      transform: translate(-1px, -1px);
      box-shadow: 3px 3px 0px var(--color-primary);
    }

    .selector-card.circlec .card-top {
      background: linear-gradient(135deg, #0F3B3F 0%, #215D60 100%);
      color: var(--color-neutral-white);
    }

    .selector-card.circlec .card-tag {
      color: #e2f1f2;
    }

    .selector-card.circlec .cta-button {
      color: #0F3B3F;
      background: #e2f1f2;
    }

    .selector-card.circlec:hover .card-top {
      background: linear-gradient(135deg, #0a272a 0%, #174547 100%);
    }

    .selector-card.circlec:hover .cta-button {
      background: var(--color-neutral-white);
      color: #0F3B3F;
      border-color: var(--color-primary);
      transform: translate(-1px, -1px);
      box-shadow: 3px 3px 0px var(--color-primary);
    }

    .selector-card.averyranch .card-top {
      background: linear-gradient(135deg, #1A365D 0%, #2B4C7E 100%);
      color: var(--color-neutral-white);
    }

    .selector-card.averyranch .card-tag {
      color: #e2edf8;
    }

    .selector-card.averyranch .cta-button {
      color: #1A365D;
      background: #e2edf8;
    }

    .selector-card.averyranch:hover .card-top {
      background: linear-gradient(135deg, #132a4b 0%, #213b63 100%);
    }

    .selector-card.averyranch:hover .cta-button {
      background: var(--color-neutral-white);
      color: #1A365D;
      border-color: var(--color-primary);
      transform: translate(-1px, -1px);
      box-shadow: 3px 3px 0px var(--color-primary);
    }

    /* COMING SOON CARD */
    .coming-soon-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background: rgba(255, 255, 255, 0.7);
      border: 2px dashed var(--color-primary);
      border-radius: 6px;
      padding: 40px;
      text-align: center;
      min-height: 290px;
      box-shadow: 4px 4px 0px rgba(15, 46, 34, 0.05);
      transition: var(--transition-smooth);
    }

    .coming-soon-card:hover {
      border-color: var(--color-accent);
      background: var(--color-accent-pale);
      transform: translate(-2px, -2px);
      box-shadow: 6px 6px 0px rgba(15, 46, 34, 0.1);
    }

    .coming-soon-card .btn-secondary {
      color: var(--color-primary);
      border-color: var(--color-primary);
      box-shadow: 3px 3px 0px var(--color-primary);
    }

    .coming-soon-card .btn-secondary:hover {
      background: var(--color-primary);
      color: var(--color-accent);
      border-color: var(--color-primary);
      transform: translate(-1px, -1px);
      box-shadow: 4px 4px 0px var(--color-primary);
    }

    .coming-soon-icon {
      font-size: 2rem;
      color: var(--color-primary-light);
      margin-bottom: 12px;
    }

    .coming-soon-text {
      font-size: 1.4rem;
      font-weight: 800;
      color: var(--color-primary);
      text-transform: uppercase;
      margin-bottom: 8px;
      letter-spacing: -0.5px;
    }

    .coming-soon-desc {
      font-size: 0.9rem;
      color: #606664;
      margin-bottom: 16px;
    }

    /* ABOUT SECTION */
    .about-section {
      background: var(--color-neutral-white);
      border-top: 1px solid var(--color-border);
      border-bottom: 1px solid var(--color-border);
    }

    .about-grid {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      gap: 80px;
      align-items: center;
    }

    .about-text h3 {
      font-size: 1.8rem;
      margin-bottom: 20px;
    }

    .about-text p {
      font-size: 1.05rem;
      color: #555c5a;
      margin-bottom: 24px;
      line-height: 1.7;
    }

    .about-highlights {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }

    .highlight-item {
      display: flex;
      align-items: flex-start;
      gap: 12px;
    }

    .highlight-icon {
      color: var(--color-accent);
      font-size: 1.2rem;
      margin-top: 2px;
    }

    .highlight-title {
      font-weight: 700;
      color: var(--color-primary);
      font-size: 0.95rem;
      margin-bottom: 4px;
    }

    .highlight-desc {
      font-size: 0.85rem;
      color: #606664;
    }

    .about-graphic {
      background: var(--color-austin-brown-tint);
      border: 1.5px solid var(--color-primary);
      padding: 40px;
      border-radius: 6px;
      position: relative;
      box-shadow: 4px 4px 0px var(--color-primary);
    }

    .about-graphic-badge {
      background: var(--color-primary);
      color: var(--color-accent);
      padding: 6px 12px;
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.65rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      border-radius: 4px;
      display: inline-block;
      margin-bottom: 16px;
      border: 1.5px solid var(--color-accent);
    }

    .about-graphic h4 {
      font-size: 1.4rem;
      margin-bottom: 12px;
    }

    .about-graphic p {
      font-size: 0.95rem;
      color: #555c5a;
      margin-bottom: 20px;
    }

    .about-graphic-bullets {
      list-style: none;
    }

    .about-graphic-bullets li {
      font-size: 0.9rem;
      color: var(--color-primary-mid);
      font-weight: 600;
      margin-bottom: 8px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .about-graphic-bullets li::before {
      content: '✓';
      color: var(--color-accent);
      font-weight: 800;
    }

    /* MEET OUR FAMILY SECTION */
    .family-section {
      background: var(--color-neutral-light);
    }

    .family-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 40px;
      margin-top: 40px;
    }

    .family-card {
      background: var(--color-neutral-white);
      padding: 40px;
      border-radius: 6px;
      border: 1.5px solid var(--color-primary);
      box-shadow: 4px 4px 0px rgba(15, 46, 34, 0.15);
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      transition: var(--transition-smooth);
    }

    .family-card:hover {
      transform: translate(-2px, -2px);
      box-shadow: 6px 6px 0px var(--color-primary);
    }

    .avatar-wrapper {
      position: relative;
      margin-bottom: 24px;
    }

    .avatar-circle {
      width: 110px;
      height: 110px;
      border-radius: 50%;
      background: var(--color-primary);
      color: var(--color-accent);
      border: 2px solid var(--color-primary);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.2rem;
      box-shadow: 3px 3px 0px var(--color-accent);
      font-family: 'Lexend Exa', sans-serif;
      font-weight: 800;
    }

    .avatar-badge {
      position: absolute;
      bottom: -5px;
      right: -5px;
      background: var(--color-accent);
      color: var(--color-primary);
      width: 32px;
      height: 32px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1rem;
      font-weight: 800;
      border: 1.5px solid var(--color-primary);
      box-shadow: 2px 2px 0px var(--color-primary);
    }

    .family-name {
      font-size: 1.4rem;
      font-weight: 700;
      margin-bottom: 4px;
    }

    .family-role {
      font-size: 0.85rem;
      font-weight: 700;
      color: var(--color-primary-light);
      text-transform: uppercase;
      letter-spacing: 1.5px;
      margin-bottom: 20px;
    }

    .family-bio {
      font-size: 0.95rem;
      color: #555c5a;
      line-height: 1.6;
    }

    /* SUGGEST A NEIGHBORHOOD / CONTACT SECTION */
    .suggest-section {
      background: var(--color-neutral-white);
      border-top: 1.5px solid var(--color-primary);
    }

    .suggest-grid {
      display: grid;
      grid-template-columns: 0.9fr 1.1fr;
      gap: 60px;
      align-items: center;
    }

    .suggest-info h3 {
      font-size: 2rem;
      margin-bottom: 20px;
      letter-spacing: -0.5px;
    }

    .suggest-info p {
      font-size: 1.05rem;
      color: #555c5a;
      margin-bottom: 30px;
      line-height: 1.6;
    }

    .suggest-contacts {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .contact-item {
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .contact-bubble {
      width: 44px;
      height: 44px;
      border-radius: 50%;
      background: var(--color-accent-pale);
      border: 1.5px solid var(--color-primary);
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--color-primary);
      font-size: 1.1rem;
      box-shadow: 2px 2px 0px var(--color-primary);
    }

    .contact-detail {
      font-size: 0.95rem;
      color: var(--color-primary-mid);
      font-weight: 600;
    }

    .contact-detail a {
      color: var(--color-primary-mid);
      text-decoration: none;
      transition: var(--transition-smooth);
    }

    .contact-detail a:hover {
      color: var(--color-accent);
      text-decoration: underline;
    }

    /* Beautiful Form Styles */
    .form-container {
      background: var(--color-neutral-light);
      border: 1.5px solid var(--color-primary);
      border-radius: 6px;
      padding: 40px;
      box-shadow: 4px 4px 0px rgba(15, 46, 34, 0.15);
    }

    .form-group {
      margin-bottom: 20px;
    }

    .form-group label {
      display: block;
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.65rem;
      font-weight: 700;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      color: var(--color-primary);
      margin-bottom: 8px;
    }

    .form-group input,
    .form-group select,
    .form-group textarea {
      width: 100%;
      padding: 12px 16px;
      border: 1.5px solid var(--color-primary);
      border-radius: 4px;
      font-family: 'Raleway', sans-serif;
      font-size: 0.95rem;
      color: var(--color-neutral-dark);
      background: var(--color-neutral-white);
      outline: none;
      transition: var(--transition-smooth);
    }

    .form-group input:focus,
    .form-group select:focus,
    .form-group textarea:focus {
      border-color: var(--color-accent);
      box-shadow: 3px 3px 0px var(--color-accent);
      background: var(--color-neutral-white);
    }

    .form-group textarea {
      resize: vertical;
      min-height: 100px;
    }

    .form-row {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }

    .form-container .btn-submit {
      width: 100%;
      background: var(--color-primary);
      color: var(--color-accent);
      border: 1.5px solid var(--color-primary);
      padding: 14px;
      font-family: 'Lexend Exa', sans-serif;
      font-size: 0.85rem;
      font-weight: 700;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      border-radius: 6px;
      cursor: pointer;
      transition: var(--transition-smooth);
      box-shadow: 3px 3px 0px var(--color-accent);
    }

    .form-container .btn-submit:hover {
      background: var(--color-accent);
      color: var(--color-primary);
      border-color: var(--color-primary);
      transform: translate(-1px, -1px);
      box-shadow: 4px 4px 0px var(--color-primary);
    }

    /* FOOTER */
    footer {
      background: #081711;
      color: rgba(255, 255, 255, 0.7);
      padding: 60px 40px;
      font-size: 0.9rem;
      border-top: 5px solid var(--color-accent);
    }

    .footer-container {
      max-width: 1200px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1.5fr 1fr;
      gap: 40px;
      align-items: center;
      margin-bottom: 40px;
    }

    .footer-brand {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .footer-logo {
      font-family: 'Lexend Exa', sans-serif;
      font-size: 1.6rem;
      font-weight: 800;
      color: var(--color-neutral-white);
      text-decoration: none;
      letter-spacing: -0.5px;
    }

    .footer-logo span {
      color: var(--color-accent);
    }

    .footer-links {
      display: flex;
      gap: 30px;
      justify-content: flex-end;
    }

    .footer-links a {
      color: rgba(255, 255, 255, 0.7);
      text-decoration: none;
      font-family: 'Lexend Exa', sans-serif;
      font-weight: 700;
      font-size: 0.72rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      transition: var(--transition-smooth);
    }

    .footer-links a:hover {
      color: var(--color-accent);
    }

    .footer-bottom {
      max-width: 1200px;
      margin: 0 auto;
      border-top: 1px solid rgba(255, 255, 255, 0.08);
      padding-top: 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.8rem;
      color: rgba(255, 255, 255, 0.4);
    }

    .footer-bottom strong {
      color: var(--color-accent);
    }

    /* ANIMATIONS */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeInRight {
      from {
        opacity: 0;
        transform: translateX(40px) rotate(0deg);
      }

      to {
        opacity: 1;
        transform: translateX(0) rotate(3deg);
      }
    }

    /* RESPONSIVE DESIGN */
    @media (max-width: 992px) {
      .hero-container {
        grid-template-columns: 1fr;
        gap: 40px;
        text-align: center;
      }

      .hero-eyebrow {
        justify-content: center;
      }

      .hero-ctas {
        justify-content: center;
      }

      .hero-graphic {
        display: none;
        /* Hide decorative mockup on tablet/mobile for cleaner layout */
      }

      .steps-grid {
        grid-template-columns: 1fr;
        gap: 20px;
      }

      .about-grid {
        grid-template-columns: 1fr;
        gap: 40px;
      }

      .family-grid {
        grid-template-columns: 1fr;
        gap: 30px;
      }

      .suggest-grid {
        grid-template-columns: 1fr;
        gap: 40px;
      }

      .footer-container {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 20px;
      }

      .footer-links {
        justify-content: center;
        flex-wrap: wrap;
      }

      .footer-bottom {
        flex-direction: column;
        gap: 12px;
        text-align: center;
      }
    }

    @media (max-width: 768px) {
      .main-nav {
        padding: 14px 20px;
        background: var(--color-neutral-white);
        backdrop-filter: none;
        -webkit-backdrop-filter: none;
      }
      .top-bar  { display: none; }

      /* ── Hamburger button visible on mobile ── */
      .menu-btn { display: inline-flex; flex-shrink: 0; }

      /* ── Prevent logo from crushing the hamburger on very narrow screens ── */
      .logo-container { min-width: 0; overflow: hidden; }
      .main-nav-logo  { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

      /* ── Burger → X morphing ── */
      .menu-toggle:checked ~ .menu-btn {
        background: var(--color-primary);
        box-shadow: none;
        border-color: var(--color-primary);
      }

      .menu-toggle:checked ~ .menu-btn .menu-bar {
        background: var(--color-accent);
      }

      .menu-toggle:checked ~ .menu-btn .menu-bar:nth-child(1) {
        transform: translateY(7px) rotate(45deg);
      }

      .menu-toggle:checked ~ .menu-btn .menu-bar:nth-child(2) {
        opacity: 0;
        width: 0;
      }

      .menu-toggle:checked ~ .menu-btn .menu-bar:nth-child(3) {
        transform: translateY(-7px) rotate(-45deg);
      }

      /* (backdrop rule moved below for correct specificity) */

      /* ── Backdrop visible on check ── */
      .menu-toggle:checked ~ .menu-backdrop {
        opacity: 1;
        pointer-events: auto;
      }

      /* ── Dropdown nav panel (right-aligned, auto height) ── */
      .nav-links {
        position: absolute;
        top: calc(100% + 4px);
        right: 16px;
        width: min(280px, calc(100vw - 32px));
        background: var(--color-neutral-white);
        flex-direction: column;
        justify-content: flex-start;
        align-items: stretch;
        gap: 0;
        padding: 8px 0;
        opacity: 0;
        transform: translateY(-8px) scale(0.97);
        transform-origin: top right;
        visibility: hidden;
        transition: opacity 0.3s cubic-bezier(0.16, 1, 0.3, 1),
                    transform 0.3s cubic-bezier(0.16, 1, 0.3, 1),
                    visibility 0s linear 0.3s;
        z-index: 101;
        box-shadow: 0 12px 40px rgba(15, 46, 34, 0.18),
                    0 2px 8px rgba(15, 46, 34, 0.08);
        border: 1.5px solid var(--color-border);
        border-radius: 10px;
        overflow: hidden;
      }

      /* Accent top-edge */
      .nav-links::before {
        content: '';
        display: block;
        height: 3px;
        background: linear-gradient(90deg, var(--color-accent), var(--color-primary-light));
        flex-shrink: 0;
      }

      .menu-toggle:checked ~ .nav-links {
        opacity: 1;
        transform: translateY(0) scale(1);
        visibility: visible;
        transition: opacity 0.32s cubic-bezier(0.16, 1, 0.3, 1),
                    transform 0.32s cubic-bezier(0.16, 1, 0.3, 1),
                    visibility 0s linear 0s;
      }

      /* ── Nav link items ── */
      .nav-links a {
        font-size: 0.75rem;
        letter-spacing: 1.5px;
        padding: 14px 22px;
        width: 100%;
        display: flex;
        align-items: center;
        text-align: left;
        border-bottom: none;
        opacity: 0;
        transform: translateY(6px);
        transition: opacity 0.25s ease, transform 0.25s ease, color 0.15s, background 0.15s;
        transition-delay: 0s;
        border-radius: 0;
        margin: 0 0;
      }

      .nav-links a::after { display: none; }

      .nav-links a:hover {
        background: var(--color-accent-pale);
        color: var(--color-primary);
      }

      .nav-links a:active {
        background: rgba(212, 175, 55, 0.15);
      }

      /* Subtle dividers between links (not the CTA) */
      .nav-links a:not(.nav-cta) + a:not(.nav-cta) {
        border-top: 1px solid rgba(226, 223, 213, 0.6);
      }

      /* ── Staggered link entrance ── */
      .menu-toggle:checked ~ .nav-links a:nth-child(1) { opacity: 1; transform: none; transition-delay: 0.08s; }
      .menu-toggle:checked ~ .nav-links a:nth-child(2) { opacity: 1; transform: none; transition-delay: 0.12s; }
      .menu-toggle:checked ~ .nav-links a:nth-child(3) { opacity: 1; transform: none; transition-delay: 0.16s; }
      .menu-toggle:checked ~ .nav-links a:nth-child(4) { opacity: 1; transform: none; transition-delay: 0.20s; }
      .menu-toggle:checked ~ .nav-links a:nth-child(5) { opacity: 1; transform: none; transition-delay: 0.24s; }

      /* ── CTA pill inside panel ── */
      .nav-links a.nav-cta {
        margin: 8px 14px 6px;
        width: calc(100% - 28px);
        text-align: center;
        display: flex;
        justify-content: center;
        border-bottom: none;
        border-top: none;
        padding: 12px 20px;
        border-radius: 6px;
        box-shadow: 2px 2px 0px var(--color-primary);
        font-size: 0.72rem;
      }

      .nav-links a.nav-cta:hover {
        background: var(--color-accent-hover) !important;
        color: var(--color-primary) !important;
        transform: translate(-1px, -1px) !important;
        box-shadow: 3px 3px 0px var(--color-primary);
      }

      /* ── Layout adjustments ── */
      section { padding: 60px 20px; }

      .about-highlights {
        grid-template-columns: 1fr;
        gap: 16px;
      }

      .form-row {
        grid-template-columns: 1fr;
        gap: 0;
      }

      .form-container { padding: 24px; }
    }

    /* === SCROLL ENTRANCE ANIMATIONS === */
    .scroll-anim {
      opacity: 0;
      transform: translateY(26px);
      transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1),
                  transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .scroll-anim.scroll-visible {
      opacity: 1;
      transform: none;
    }

    /* Stagger siblings within their grids */
    .steps-grid    .step-card.scroll-anim:nth-child(2)     { transition-delay: 0.1s; }
    .steps-grid    .step-card.scroll-anim:nth-child(3)     { transition-delay: 0.2s; }
    .panel-grid    .selector-card.scroll-anim:nth-child(2) { transition-delay: 0.1s; }
    .panel-grid    .selector-card.scroll-anim:nth-child(3) { transition-delay: 0.2s; }
    .panel-grid    .selector-card.scroll-anim:nth-child(4),
    .panel-grid    .coming-soon-card.scroll-anim           { transition-delay: 0.3s; }
    .family-grid   .family-card.scroll-anim:nth-child(2)   { transition-delay: 0.12s; }
    .about-highlights .highlight-item.scroll-anim:nth-child(2) { transition-delay: 0.08s; }
    .about-highlights .highlight-item.scroll-anim:nth-child(3) { transition-delay: 0.16s; }
    .about-highlights .highlight-item.scroll-anim:nth-child(4) { transition-delay: 0.24s; }
    .trust-stats .trust-stat.scroll-anim:nth-child(2)          { transition-delay: 0.12s; }
    .trust-stats .trust-stat.scroll-anim:nth-child(3)          { transition-delay: 0.24s; }
    .trust-bar   .trust-tagline.scroll-anim                    { transition-delay: 0.34s; }

    /* Formspree Submission & Loading/Success Animations */
    .btn-submit {
      position: relative;
    }

    .btn-submit .btn-spinner {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      justify-content: center;
    }

    .spinner-svg {
      animation: rotateSpinner 0.8s linear infinite;
      display: inline-block;
      vertical-align: middle;
    }

    @keyframes rotateSpinner {
      100% {
        transform: rotate(360deg);
      }
    }

    /* Success State Styling */
    .form-success-state {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      animation: fadeInUp 0.5s cubic-bezier(0.25, 0.8, 0.25, 1) forwards;
      padding: 20px 0;
    }

    .form-success-state h4 {
      font-size: 1.8rem;
      color: var(--color-primary);
      margin-top: 20px;
      margin-bottom: 12px;
    }

    .form-success-state p {
      font-size: 1.05rem;
      color: #555c5a;
      max-width: 400px;
      line-height: 1.6;
    }

    /* SVG Checkmark Animation */
    .success-icon-wrapper {
      width: 80px;
      height: 80px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .checkmark-svg {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      display: block;
      stroke-width: 3;
      stroke: var(--color-primary);
      stroke-miterlimit: 10;
      box-shadow: inset 0px 0px 0px var(--color-accent);
      animation: fillSuccess 0.4s ease-in-out 0.4s forwards, scaleSuccess 0.3s ease-in-out 0.9s both;
    }

    .checkmark-circle {
      stroke-dasharray: 166;
      stroke-dashoffset: 166;
      stroke-width: 3;
      stroke-miterlimit: 10;
      stroke: var(--color-primary);
      fill: none;
      animation: strokeCircle 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
    }

    .checkmark-check {
      transform-origin: 50% 50%;
      stroke-dasharray: 48;
      stroke-dashoffset: 48;
      stroke-width: 4;
      stroke: var(--color-primary);
      animation: strokeCheck 0.3s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
    }

    @keyframes strokeCircle {
      100% {
        stroke-dashoffset: 0;
      }
    }

    @keyframes strokeCheck {
      100% {
        stroke-dashoffset: 0;
      }
    }

    @keyframes fillSuccess {
      100% {
        box-shadow: inset 0px 0px 0px 80px var(--color-accent);
      }
    }

    @keyframes scaleSuccess {

      0%,
      100% {
        transform: none;
      }

      50% {
        transform: scale3d(1.1, 1.1, 1);
      }
    }
  </style>
</head>

<body>

  <!-- Top Utility Bar -->
  <div class="top-bar">
    <span>🌿 hyper-local &nbsp;·&nbsp; 100% neighbor-sourced referrals</span>
    <div class="top-bar-links">
      <a href="#how-it-works">How It Works</a>
      <a href="#suggest-neighborhood">Add Neighborhood</a>
    </div>
  </div>

  <!-- Navigation Bar (Glassmorphic) -->
  <nav class="main-nav">
    <a href="/" class="logo-container">
      <div class="logo-badge">T</div>
      <h1 class="main-nav-logo">Trusted<span>Neighbors</span></h1>
    </a>

    <input type="checkbox" id="menu-toggle" class="menu-toggle">
    <label for="menu-toggle" class="menu-btn" id="menuBtnLabel" aria-label="Toggle navigation">
      <span class="menu-bar"></span>
      <span class="menu-bar"></span>
      <span class="menu-bar"></span>
    </label>
    <div class="menu-backdrop" id="menuBackdrop" onclick="closeMobileMenu()"></div>

    <div class="nav-links" id="navLinks">
      <a href="#neighborhoods" onclick="closeMobileMenu()">Neighborhoods</a>
      <a href="#how-it-works" onclick="closeMobileMenu()">How it works</a>
      <a href="#about" onclick="closeMobileMenu()">About</a>
      <!-- <a href="#meet-our-family" onclick="closeMobileMenu()">Meet Our Family</a> -->
      <a href="#suggest-neighborhood" class="nav-cta" onclick="closeMobileMenu()">Add Neighborhood</a>
    </div>
  </nav>

  <!-- Hero Section -->
  <header class="hero">
    <div class="hero-container">
      <div class="hero-content">
        <div class="hero-eyebrow">
          <span>🌿</span> Community Referral Platform
        </div>
        <h2>Directories Built on <span>Real Trust</span>.</h2>
        <p class="hero-subtitle">
          Skip the sponsored ads, premium search game, and fake online reviews. Find authentic, hand-picked local
          businesses recommended by your immediate neighbors.
        </p>
        <div class="hero-ctas">
          <a href="#neighborhoods" class="btn btn-primary">Select Directory</a>
          <a href="#how-it-works" class="btn btn-secondary">Learn More</a>
        </div>
      </div>
      <div class="hero-graphic">
        <div class="hero-image-card">
          <div class="badge-floating">
            <span>Verified</span>
            <strong>100%</strong>
            <span>Local</span>
          </div>
        </div>
      </div>
    </div>
  </header>

  <!-- Trust / Stats Bar -->
  <section class="trust-bar">
    <div class="trust-bar-inner">
      <span class="trust-eyebrow"><span>🌿</span> Trusted by Local Neighbors</span>
      <div class="trust-stats">
        <div class="trust-stat">
          <span class="trust-number" data-count="400" data-suffix="+">400+</span>
          <span class="trust-label">Recommendations</span>
        </div>
        <div class="trust-stat">
          <span class="trust-number" data-count="100" data-suffix="+">100+</span>
          <span class="trust-label">Local Businesses</span>
        </div>
        <div class="trust-stat">
          <span class="trust-number" data-count="4" data-suffix="">4</span>
          <span class="trust-label">Active Neighborhoods</span>
        </div>
      </div>
      <p class="trust-tagline">Discover trusted businesses recommended by people in your community.</p>
    </div>
  </section>

  <!-- How It Works Section -->
  <section id="how-it-works" class="how-it-works">
    <div class="section-container">
      <div class="section-header">
        <span class="section-eyebrow">The System</span>
        <h3 class="section-title">How Trusted Neighbors Works</h3>
        <p class="section-desc">We built a simple, community-first pipeline to surface the best vendors without paid
          bias.</p>
      </div>

      <div class="steps-grid">
        <!-- Step 1 -->
        <div class="step-card">
          <span class="step-number">01</span>
          <div class="step-icon">💬</div>
          <h4 class="step-title">Organic Discovery</h4>
          <p class="step-text">We actively monitor private neighborhood Facebook groups and Nextdoor threads where
            neighbors post spontaneous, unprompted business recommendations.</p>
        </div>

        <!-- Step 2 -->
        <div class="step-card">
          <span class="step-number">02</span>
          <div class="step-icon">🛡️</div>
          <h4 class="step-title">Manual Verification</h4>
          <p class="step-text">Our dedicated Virtual Assistants check every single entry manually. We filter out spam,
            fake profiles, and self-promotion to ensure only genuine referrals are logged.</p>
        </div>

        <!-- Step 3 -->
        <div class="step-card">
          <span class="step-number">03</span>
          <div class="step-icon">📞</div>
          <h4 class="step-title">Direct Connection</h4>
          <p class="step-text">Browse neat, category-organized directories. We provide clean recommendation quotes and
            tap-to-call numbers so you can contact trusted pros instantly.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Select Neighborhood Section -->
  <section id="neighborhoods" class="neighborhoods-section">
    <div class="section-container">
      <div class="section-header">
        <span class="section-eyebrow">Directories</span>
        <h3 class="section-title">Select Your Neighborhood</h3>
        <p class="section-desc">Open the specific community directory below to find local recommendations near you.</p>
      </div>

      <div class="panel-grid">
        <!-- Onion Creek Card -->
        <a class="selector-card onion" href="/onion-creek/">
          <div class="card-top">
            <div>
              <div class="card-tag">Austin, TX</div>
              <h4 class="card-title">Onion Creek</h4>
            </div>
            <p class="card-desc">Featuring verified plumbers, roofers, landscapers, and tailors recommended by Onion
              Creek residents.</p>
          </div>
          <div class="card-footer">
            <span class="cta-label">Active Directory</span>
            <div class="cta-button">Open Directory</div>
          </div>
        </a>

        <!-- Sunfield Card -->
        <a class="selector-card sunfield" href="/sunfield/">
          <div class="card-top">
            <div>
              <div class="card-tag">Buda, TX</div>
              <h4 class="card-title">Sunfield</h4>
            </div>
            <p class="card-desc">Sourced directly from Buda homeowners. Safe and vetted local services for your
              household.</p>
          </div>
          <div class="card-footer">
            <span class="cta-label">Active Directory</span>
            <div class="cta-button">Open Directory</div>
          </div>
        </a>

        <!-- Circle C Card -->
        <a class="selector-card circlec" href="/circle-c/">
          <div class="card-top">
            <div>
              <div class="card-tag">Austin, TX</div>
              <h4 class="card-title">Circle C</h4>
            </div>
            <p class="card-desc">Trusted local resources, handymen, and kids activities recommended in Circle C Austin
              feeds.</p>
          </div>
          <div class="card-footer">
            <span class="cta-label">Active Directory</span>
            <div class="cta-button">Open Directory</div>
          </div>
        </a>

        <!-- Avery Ranch Card -->
        <a class="selector-card averyranch" href="/avery-ranch/">
          <div class="card-top">
            <div>
              <div class="card-tag">Austin, TX</div>
              <h4 class="card-title">Avery Ranch</h4>
            </div>
            <p class="card-desc">Sourced directly from Avery Ranch homeowners. Vetted plumbers, landscapers, and home life services for your household.</p>
          </div>
          <div class="card-footer">
            <span class="cta-label">Active Directory</span>
            <div class="cta-button">Open Directory</div>
          </div>
        </a>

        <!-- Coming Soon / Suggest Card -->
        <div class="coming-soon-card">
          <div class="coming-soon-icon">📍</div>
          <h4 class="coming-soon-text">More Coming Soon</h4>
          <p class="coming-soon-desc">We are continuously reviewing recommendations to launch new neighborhoods.</p>
          <a href="#suggest-neighborhood" class="btn btn-secondary">Suggest Yours</a>
        </div>
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section id="about" class="about-section">
    <div class="section-container">
      <div class="about-grid">
        <div class="about-text">
          <span class="section-eyebrow">Our Philosophy</span>
          <h3>Reclaiming Community Word-of-Mouth</h3>
          <p>
            Yelp, Angi, and Google reviews were once useful tools, but today they are heavily gamed by search engine
            optimization (SEO) teams, fake profile rings, and sponsored bidding wars. The contractor at the top is
            rarely the best—they are simply the one who paid the most.
          </p>
          <p>
            <strong>Trusted Neighbors</strong> restores authenticity. Our listings are completely free for basic
            insertion and are selected entirely because a real neighbor went out of their way to recommend them. No
            bias, no corporate manipulation.
          </p>

          <div class="about-highlights">
            <div class="highlight-item">
              <span class="highlight-icon">🛡️</span>
              <div>
                <h5 class="highlight-title">Zero Sponsored Noise</h5>
                <p class="highlight-desc">Basic listings cannot be bought. Authenticity is our entire value proposition.
                </p>
              </div>
            </div>
            <div class="highlight-item">
              <span class="highlight-icon">🏡</span>
              <div>
                <h5 class="highlight-title">Strictly Local</h5>
                <p class="highlight-desc">Vetted lists customized specifically for active HOAs and subdivisions.</p>
              </div>
            </div>
          </div>
        </div>

        <div class="about-graphic">
          <span class="about-graphic-badge">Physical Directory</span>
          <h4>The Printed Edition</h4>
          <p>
            Our online platform is backed by our premium yardage book-style printed directory. Designed to live
            permanently on the fridge via a magnetic back, it keeps local numbers exactly where you need them.
          </p>
          <ul class="about-graphic-bullets">
            <li>Green leatherette cover with elegant gold lettering</li>
            <li>Slip-in seasonal inserts (Spring / Pre-Holiday)</li>
            <li>Hand-delivered door-to-door twice a year</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Meet Our Family Section (Hidden for now)
  <section id="meet-our-family" class="family-section">
    <div class="section-container">
      <div class="section-header">
        <span class="section-eyebrow">The Team</span>
        <h3 class="section-title">Meet Our Family</h3>
        <p class="section-desc">We are a local family business dedicated to serving Central Texas communities.</p>
      </div>

      <div class="family-grid">
        [Seth Card]
        <div class="family-card">
          <div class="avatar-wrapper">
            <div class="avatar-circle">SH</div>
            <div class="avatar-badge">🌿</div>
          </div>
          <h4 class="family-name">Seth Hill</h4>
          <div class="family-role">Founder &amp; Coordinator</div>
          <p class="family-bio">
            Seth coordinates directory infrastructure, runs QA on the virtual assistant data pipelines, and oversees
            vendor partnerships. As a long-term area resident, he wanted to build a clean resource to help homeowners
            support real, high-quality local businesses.
          </p>
        </div>

        [Kids Card]
        <div class="family-card">
          <div class="avatar-wrapper">
            <div class="avatar-circle">HK</div>
            <div class="avatar-badge">📦</div>
          </div>
          <h4 class="family-name">The Hill Kids</h4>
          <div class="family-role">Sales &amp; Distribution</div>
          <p class="family-bio">
            The heart and muscle of the physical operation! The kids manage inventory, package the printed inserts into
            their leatherette magnetic covers, and lead the seasonal door-to-door distribution, delivering the directory
            straight to your doorstep.
          </p>
        </div>
      </div>
    </div>
  </section>
  -->

  <!-- Suggest Neighborhood / Form Section -->
  <section id="suggest-neighborhood" class="suggest-section">
    <div class="section-container">
      <div class="suggest-grid">
        <div class="suggest-info">
          <span class="section-eyebrow">Expansion</span>
          <h3>Bring Trusted Neighbors to Your Area</h3>
          <p>
            Don't see your subdivision or neighborhood listed? We expand based on community demand and volunteer
            moderator engagement. Fill out the request form to put your community on our launch queue.
          </p>
          <div class="suggest-contacts">
            <div class="contact-item">
              <div class="contact-bubble">📧</div>
              <div>
                <span
                  style="font-size: 0.75rem; text-transform: uppercase; color: #606664; display: block; font-weight: 700;">General
                  Inquiries</span>
                <span class="contact-detail">trustedneighbors.marc@gmail.com</span>
              </div>
            </div>
            <div class="contact-item">
              <div class="contact-bubble">📍</div>
              <div>
                <span
                  style="font-size: 0.75rem; text-transform: uppercase; color: #606664; display: block; font-weight: 700;">Headquarters</span>
                <span class="contact-detail">Austin, Texas</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Suggest Form (uses Formspree) -->
        <div class="form-container">
          <form id="suggestionForm" action="https://formspree.io/f/xkoerowv" method="POST">

            <div class="form-row">
              <div class="form-group">
                <label for="name">Your Name *</label>
                <input type="text" id="name" name="name" placeholder="e.g. Sarah Jennings" required />
              </div>
              <div class="form-group">
                <label for="email">Email Address *</label>
                <input type="email" id="email" name="email" placeholder="e.g. sarah@example.com" required />
              </div>
            </div>

            <div class="form-group">
              <label for="neighborhood">Neighborhood &amp; City *</label>
              <input type="text" id="neighborhood" name="neighborhood"
                placeholder="e.g. Circle C, Austin or Plum Creek, Buda" required />
            </div>

            <div class="form-group">
              <label for="role">Who Are You? *</label>
              <select id="role" name="role" required>
                <option value="" disabled selected>— Select your role —</option>
                <option value="resident">Neighborhood Resident</option>
                <option value="hoa-board">HOA Board Member / Volunteer</option>
                <option value="vendor">Local Business Vendor</option>
                <option value="other">Other</option>
              </select>
            </div>

            <div class="form-group">
              <label for="message">Why should we launch in your area? *</label>
              <textarea id="message" name="message"
                placeholder="e.g. Our Facebook group has 2,000 active members asking for contractor referrals daily..."
                required></textarea>
            </div>

            <button type="submit" class="btn-submit" id="submitBtn">
              <span class="btn-text">Submit Request</span>
              <span class="btn-spinner" style="display: none; align-items: center; gap: 8px; justify-content: center;">
                <svg class="spinner-svg" viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor"
                  stroke-width="3">
                  <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="3" opacity="0.2"></circle>
                  <path d="M12 2 a 10 10 0 0 1 10 10" stroke="currentColor" stroke-width="3" stroke-linecap="round">
                  </path>
                </svg>
                Sending...
              </span>
            </button>
          </form>

          <!-- Success State (hidden by default) -->
          <div id="formSuccess" class="form-success-state" style="display: none;">
            <div class="success-icon-wrapper">
              <svg class="checkmark-svg" viewBox="0 0 52 52">
                <circle class="checkmark-circle" cx="26" cy="26" r="25" fill="none" />
                <path class="checkmark-check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8" />
              </svg>
            </div>
            <h4>Request Received!</h4>
            <p>Your suggestions have been successfully logged. We will review them and reach out as we build our launch
              queue!</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <div class="footer-container">
      <div class="footer-brand">
        <a href="/" class="footer-logo">Trusted<span>Neighbors</span></a>
        <p style="font-size: 0.85rem; color: rgba(255,255,255,0.4); max-width: 400px; line-height: 1.5;">
          Hyper-local neighborhood referral directories. Sourced by residents, verified by our team, and presented
          without corporate sponsored clutter.
        </p>
      </div>
      <div class="footer-links">
        <a href="#neighborhoods">Directories</a>
        <a href="#how-it-works">How it works</a>
        <a href="#about">About Us</a>
        <!-- <a href="#meet-our-family">The Family</a> -->
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; 2026 <strong>Trusted Neighbors</strong> &nbsp;·&nbsp; Austin, TX &nbsp;·&nbsp; All rights reserved.</p>
      <p>Domain: <a href="http://trustedneighbors.net"
          style="color: var(--color-accent); text-decoration: none;">trustedneighbors.net</a></p>
    </div>
  </footer>

  <script is:inline>
    // ── Navigation toggle ──
    const menuToggle = document.getElementById('menu-toggle');

    function closeMobileMenu() {
      if (menuToggle) menuToggle.checked = false;
    }

    // ── Scroll entrance animations ──
    (function () {
      const targets = document.querySelectorAll(
        '.step-card, .selector-card, .coming-soon-card, .family-card, ' +
        '.section-header, .about-graphic, .about-text, ' +
        '.highlight-item, .suggest-info, .form-container, ' +
        '.trust-eyebrow, .trust-stat, .trust-tagline'
      );

      targets.forEach(el => el.classList.add('scroll-anim'));

      if ('IntersectionObserver' in window) {
        const obs = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('scroll-visible');
              obs.unobserve(entry.target);
            }
          });
        }, { threshold: 0.08, rootMargin: '0px 0px -28px 0px' });

        targets.forEach(el => obs.observe(el));
      } else {
        targets.forEach(el => el.classList.add('scroll-visible'));
      }
    })();

    // ── Count-up animation for trust/stats numbers ──
    (function () {
      const nums = document.querySelectorAll('.trust-number[data-count]');
      if (!nums.length) return;

      const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      // No-JS already shows final values; bail (leave them) for reduced-motion or old browsers.
      if (reduceMotion || !('IntersectionObserver' in window)) return;

      const DURATION = 2400; // ~2.4s
      const easeOutCubic = t => 1 - Math.pow(1 - t, 3);

      function run(el) {
        const target = parseFloat(el.dataset.count) || 0;
        const suffix = el.dataset.suffix || '';
        const start = performance.now();
        el.textContent = '0' + suffix;
        function tick(now) {
          const p = Math.min((now - start) / DURATION, 1);
          el.textContent = Math.round(easeOutCubic(p) * target).toLocaleString() + suffix;
          if (p < 1) requestAnimationFrame(tick);
        }
        requestAnimationFrame(tick);
      }

      const obs = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            run(entry.target);
            obs.unobserve(entry.target);
          }
        });
      }, { threshold: 0.4 });

      nums.forEach(el => obs.observe(el));
    })();

    // ── Formspree AJAX Submission with Animation ──
    const suggestionForm = document.getElementById('suggestionForm');
    const formSuccess = document.getElementById('formSuccess');
    const submitBtn = document.getElementById('submitBtn');
    const btnText = submitBtn.querySelector('.btn-text');
    const btnSpinner = submitBtn.querySelector('.btn-spinner');

    if (suggestionForm) {
      suggestionForm.addEventListener('submit', async function (e) {
        e.preventDefault();

        // Disable button & show spinner
        submitBtn.disabled = true;
        btnText.style.display = 'none';
        btnSpinner.style.display = 'inline-flex';
        submitBtn.style.opacity = '0.8';

        const formData = new FormData(suggestionForm);

        try {
          const response = await fetch(suggestionForm.action, {
            method: 'POST',
            body: formData,
            headers: {
              'Accept': 'application/json'
            }
          });

          if (response.ok) {
            // Smoothly transition the form out
            suggestionForm.style.transition = 'opacity 0.4s ease, transform 0.4s ease';
            suggestionForm.style.opacity = '0';
            suggestionForm.style.transform = 'scale(0.95)';

            setTimeout(() => {
              suggestionForm.style.display = 'none';
              formSuccess.style.display = 'flex';
            }, 400);
          } else {
            const data = await response.json();
            if (data && data.errors) {
              alert('Error: ' + data.errors.map(err => err.message).join(', '));
            } else {
              alert('Oops! There was a problem submitting your form.');
            }
            resetSubmitButton();
          }
        } catch (error) {
          alert('Oops! There was a network connectivity issue.');
          resetSubmitButton();
        }
      });
    }

    function resetSubmitButton() {
      submitBtn.disabled = false;
      btnText.style.display = 'inline-block';
      btnSpinner.style.display = 'none';
      submitBtn.style.opacity = '1';
    }
  </script>
</body>

</html>